package identitylogger.util;

import java.nio.file.Path;
import java.nio.file.Paths;

public class FileHandler {
    public FileHandler(String prevPath, String newName) {
        Path srcPath = Paths.get(prevPath);
    }
}
